# How we work

## Test, Deploy, Release

* We release when we have something to release, following the principle of continuous delivery.
* We test new functionality in the `dev` environment to make sure features have been tested and
free of bugs before releasing to `prod`.
